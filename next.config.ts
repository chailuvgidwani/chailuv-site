import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Placeholder imagery ships locally in /public/placeholders as SVGs.
  // When real photographs are added as raster files you can switch the
  // components to next/image; no remotePatterns are needed for local assets.
};

export default nextConfig;
